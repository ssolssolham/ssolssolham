기본 데이터 타입(8개) - 정수형,실수형,논리형

참조 데이터 타입(레퍼런스)

char : 2 byte(16bit) : 2n - 1

유니코드 : 
아스키코드 : 

char형 변수에 문자에 해당하는 아스키코드나 유니코드를 직접 할당할 수 있다

23페이지 (중요)
char 형에는 모니터, 프린터 같은 주변기기를 제어하는데 사용하는 제어 문자와 인용부호(“”) 안에서 표현될 수 없는 특수한 문자(“, ‘, \)를 표현하는데 사용되는 이스케이프(Escape) 문자가 있다.

특수문자
제어문자
이스케이프 문자

8진수 : 0다음 0~7까지의 숫자들로 표현 ex) 07
16진수 : ex) 0X1F 

할당연산자

모든 연산자들은 연산자의 타입이 동일해야 할당가능

형변환(캐스팅)

%d

상수로 소수형과 지수형을 사용할 수 있다.
 1.2345E3;

주의사항 보기

f

연산자

instanceof
조건삼항연산자
배열연산자

int i : array

연산자 우선순위 : 1등과 꼴등
()[]. , =

복합 대입 연산자

자동 형변화(Promotion) : Up Casting
작은 데이터타입에서 큰 데이터타입으로의 형변환은 자동적으로 이루어 진다.
데이터의 손실이 발생하지 않는다.

강제 형변화(Demotion) - Down Casting
큰 데이터타입에서 작은 데이터타입으로 형변환(뒤를 버리고 간다)
데이터의 손실이 발생할 수 있다.

비트연산자
비트 논리 연산자 : &,|,^,~
논리 연산자 : &&, ||, !

| 하나는 뒤까지 수행처리(퍼포먼스의 차이) 에러는 안남
|| 앞의것이 참이면 수행 끝

조건 삼항 연산자 : 조건 ? 수식1 : 수식2;
최대값 구하는 연습 : max = (a > b ) ? a : b;

제어문 : 조건문,반복문,이동문

첫 과제 : 별

enum 

switch(정수식)  : 실수는 안된다.

do while문


continue문
break문

레이블(Label) 선언으로 C언어의 GOTO처럼 사용할 수 있다.

FOR문의 레이블 선언
		
non-static method cannot be referenced from a static context;
